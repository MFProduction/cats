<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
        <div class="navbar-brand">Cemetery</div>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if customer_signed_in? %>
          <li><%= link_to("Funerals", funerals_path , class: "") %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if customer_signed_in? %>
          <li><%= link_to "#{current_customer.email}", edit_customer_registration_path, class: "" %></li>
          <li><%= link_to "Logout", destroy_customer_session_path, method: :delete, class: "" %></li>
        <% else %>
          <li><%= link_to("Login", new_customer_session_path, class: "") %></li>
          <li><%= link_to("Sign up", new_customer_registration_path , class: "") %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
